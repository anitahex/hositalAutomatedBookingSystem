fastapi
uvicorn
python-multipart>=0.0.9
pydantic
email-validator
python-dotenv
fasttext-wheel>=0.9.2
numpy<2
# fasttext-wheel 0.9.2 requires NumPy 1.x; keep the existing RAG stack compatible.
scipy<1.14
scikit-learn<1.6

# Hugging Face — agents, routing, answer composition
huggingface-hub
langchain-core
langchain-huggingface
sentence-transformers

# LangGraph orchestration
langgraph

# Database
psycopg2-binary
alembic
pandas

# PDF / image extraction
pdf2image
pdfplumber
pymupdf
pillow

# Vector DB — global clinical-reference RAG only
qdrant-client

# OpenAI API — ingestion path only (Guardrail 1 + Tier 2 extraction)
openai>=1.0

# Azure Blob Storage — vault + summary JSON
azure-storage-blob>=12.0
aiohttp

# WebSocket support
websockets

# Testing
pytest
pyotp
cryptography
